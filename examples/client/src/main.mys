from websocket.client import Client

def main():
    client = Client("ws://localhost:20000")
    client.connect()
    self.websocket.send_text("Hello!")
    print(self.websocket.receive_text())
    connect.disconnect()
